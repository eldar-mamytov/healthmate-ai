# Python
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
venv/
pip-log.txt
pip-delete-this-directory.txt
.venv/
.pytest_cache/
.mypy_cache/
SECRET_KEY.txt
keys.txt
/database/

# Node.js / React
node_modules/
build/ # The output of npm run build for the frontend
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.parcel-cache/
dist/

# Docker
*.env # For any .env files that might be created at the root or within subdirectories (excluding your custom .env)
docker-compose.override.yml # If you create local overrides
!.gitignore # Don't ignore the .gitignore itself
!.gitkeep # Don't ignore empty directories marker files
*.iml # IntelliJ IDEA files

# OS/IDE specific files
.DS_Store # macOS
.vscode/ # VS Code settings, but consider if you want to share workspace settings
.idea/ # IntelliJ/PyCharm IDEA files
Thumbs.db # Windows